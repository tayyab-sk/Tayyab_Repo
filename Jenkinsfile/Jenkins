pipeline {
    agent any
    stages {
        stage('Check Running Services') {
            steps {
                sh 'systemctl list-units --type=service --state=running'
            }
        }
    }
}
